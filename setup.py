# !/usr/bin/env python

from distutils.core import setup

setup(name='nicknames',
      version='0.01',
      packages=['nicknames'],
      install_requires=[],
      package_dir={'nicknames': 'nicknames'}
      )